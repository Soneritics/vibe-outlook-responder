/**
 * GenerateScreen component - Prompt selection and AI generation for compose mode
 * Features:
 * - List of available prompts
 * - One-click generation
 * - Progress overlay during generation
 * - Error handling with retry
 */
import React from 'react';
interface GenerateScreenProps {
    onOpenSettings?: () => void;
    onAddPrompt?: () => void;
}
export declare const GenerateScreen: React.FC<GenerateScreenProps>;
export {};
//# sourceMappingURL=GenerateScreen.d.ts.map